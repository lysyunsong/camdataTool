spring:
  datasource:
    driver-class-name: oracle.jdbc.OracleDriver
    url: jdbc:oracle:thin:@192.168.7.237:19290:REP
    username: CAMD_3S
    password: Buja5wEYtjxkunMj
    hikari:
      maximum-pool-size: 10
      connection-timeout: 0
      idle-timeout: 0
      max-lifetime: 0

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.part.data.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    default-fetch-size: 100
    default-statement-timeout: 0

server:
  port: 8080
  servlet:
    context-path: /part-system
# Swagger3配置（OpenAPI）
springdoc:
  api-docs:
    path: /api-docs  # API文档JSON路径
    groups:
      enabled: true
  swagger-ui:
    path: /swagger-ui.html  # Swagger UI访问路径（完整访问地址：http://localhost:8081/component/swagger-ui.html）
    operationsSorter: method  # 按HTTP方法排序接口
    tagsSorter: alpha  # 按标签字母排序
    packages-to-scan: com.part.controller  # 扫描控制器包（生成API文档）
# 日志配置
logging:
  level:
    root: INFO
    com.part: DEBUG
    com.part.data.mapper: DEBUG
  file:
    name: logs/part-system.log
    max-size: 10MB
    max-history: 30

# 模型训练相关配置
model:
  training:
    data:
      min-sample-size: 100  # 最小训练样本数
      batch-size: 32        # 批处理大小
    parameters:
      learning-rate: 0.05
      max-iterations: 100
      num-leaves: 31
  save-path: models/  # 模型保存路径
    